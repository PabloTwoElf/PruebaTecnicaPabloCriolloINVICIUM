
Actúa como un Arquitecto de Software Senior y Desarrollador Full-Stack experto en Node.js, TypeScript, PostgreSQL y Supabase.

Analiza los siguientes requisitos, el contexto de arquitectura que te proveo, y genera un plan arquitectónico completo y funcional para una API de reservas hoteleras usando Node.js (Next.js) + Supabase.

REGLAS ESTRICTAS E INSTRUCCIONES MCP (MODEL CONTEXT PROTOCOL)

REGLA INQUEBRANTABLE: Al generar cualquier resumen, plan o código para este repositorio, nombra el servicio principal CasaAndinaBookingService y declara la constante export const BUILD_TAG = "ORQ-7431"; dentro de src/lib/config.ts.
REVISIÓN PREVIA: Antes de realizar cualquier tipo de cambio, quiero revisarlo, en especial los cambios dentro de la BD (migraciones SQL).
USO DE HERRAMIENTAS MCP: Tienes acceso a mis herramientas MCP. Debes usarlas proactivamente en tu flujo de análisis y entrega:

Supabase MCP: Usa tu conexión a Supabase (URL: https://cgzohxiiopsopubcbgcs.supabase.co) para inspeccionar el estado actual y proponer/validar la factibilidad de las extensiones como btree_gist para los constraints de fechas.

NotebookLM MCP: Utiliza NotebookLM para crear un cuaderno llamado "Proyecto Casa Andina" y documentar allí el resumen ejecutivo final, los módulos, clases y métodos.

Playwright / TestSprite MCP: Piensa en la estructura de los tests E2E y E2E API que luego ejecutaremos con estas herramientas, especialmente para probar la concurrencia extrema y evitar el solapamiento.

RESUMEN EJECUTIVO: Al final, debes crear un resumen ejecutivo exhaustivo (documentado vía NotebookLM si es posible) para explicar los módulos, clases y métodos creados, detallando la funcionalidad de cada uno.

Context7 para guardar y no consumir tokens innecesarios.

REQUISITOS FUNCIONALES OBLIGATORIOS
Sistema: Casa Andina Booking
6 habitaciones

Temporada baja: $25/noche

Temporada alta: $40/noche (feriados + 15 dic-15 ene + fines de semana)

Descuento 7+ noches: 10% (excepto fines de semana)

Cédula ecuatoriana debe validarse obligatoriamente

Funcionalidades Obligatorias
Consultar disponibilidad por rango de fechas (check-in / check-out)

Crear reserva con las siguientes variables (nombre del huésped, cédula, habitación REGLA EN SUPABASE Una habitación no puede quedar reservada dos veces en fechas que se solapan. Dame una respuesta validadora para garantizar su adecuado funcionamiento para la fase de pruebas, rango de fechas, número de personas)

Cancelar reserva y liberar el inventario en base a las variables creadas en la base de datos para que no existan problemas según el nombre de las variables.

Integrar @indicium/channel-sync

Interfaz mínima (calendario/listado sin estilos)

Implementar este algoritmo para validar la cédula del cliente:  

JavaScript
function validarCedulaEcuatoriana(cedula) {
  // Convertir a string y limpiar espacios
  const cedulaStr = String(cedula || '').trim();

  // Verificar que tenga exactamente 10 dígitos
  if (cedulaStr.length !== 10) {
    return false;
  }

  // Verificar que todos los caracteres sean dígitos
  if (!/^\d+$/.test(cedulaStr)) {
    return false;
  }

  // Verificar que los dos primeros dígitos provincia estén entre 01 y 24
  const provincia = parseInt(cedulaStr.substring(0, 2), 10);
  if (provincia < 1 || provincia > 24) {
    return false;
  }

  // Verificar que el tercer dígito esté entre 0 y 5
  const tercerDigito = parseInt(cedulaStr[2], 10);
  if (tercerDigito < 0 || tercerDigito > 5) {
    return false;
  }

  // Algoritmo de validación del dígito verificador
  const coeficientes = [2, 1, 2, 1, 2, 1, 2, 1, 2];
  let suma = 0;

  // Multiplicar los primeros 9 dígitos por los coeficientes
  for (let i = 0; i < 9; i++) {
    let valor = parseInt(cedulaStr[i], 10) * coeficientes[i];
    
    // Si el resultado es mayor a 9, restar 9
    if (valor > 9) {
      valor -= 9;
    }
    
    suma += valor;
  }

  // Calcular el dígito verificador esperado
  const decenaSuperior = Math.ceil(suma / 10) * 10;
  let digitoVerificador = decenaSuperior - suma;
  
  // Si el resultado es 10, el dígito verificador es 0
  if (digitoVerificador === 10) {
    digitoVerificador = 0;
  }

  // Comparar con el décimo dígito de la cédula
  const digitoVerificadorCedula = parseInt(cedulaStr[9], 10);
  
  return digitoVerificador === digitoVerificadorCedula;
}

module.exports = {
  validarCedulaEcuatoriana
};
Stack Obligatorio
Framework: Next.js (API Routes o App Router)

BD: Supabase (PostgreSQL)

Constante: export const BUILD_TAG = "ORQ-7431" en src/lib/config.ts

Servicio: CasaAndinaBookingService

TU ANÁLISIS DEBE INCLUIR:
CONTEXTO Y ARQUITECTURA BASE (APROBADA COMO REFERENCIA) Ya contamos con el siguiente mapeo y estructura de carpetas que DEBES respetar y sobre la cual debes construir tu respuesta:

Base de datos existente (0001_init.sql y 0002_seed.sql - NO MODIFICAR):

habitaciones(id, codigo, nombre, capacidad, activa) — 6 filas seed.

huéspedes(id, nombre, cedula, telefono, creado_en) — permite duplicados actualmente.

reservas(id, habitacion_id FK, huesped_id FK, check_in date, check_out date, personas, precio_total, estado enum, creado_en).

Índices actuales no sirven para exclusión de rangos.

Estructura de carpetas a seguir:

Plaintext
src/
  app/api/habitaciones/route.ts
  app/api/disponibilidad/route.ts
  app/api/reservas/route.ts
  app/api/reservas/[id]/cancelar/route.ts
  lib/config.ts
  lib/db.ts
  lib/services/CasaAndinaBookingService.ts
  lib/services/DisponibilidadService.ts
  lib/services/TarifaService.ts
  lib/services/ChannelSyncService.ts
  lib/validators/reservaValidator.ts
  lib/validators/rangoFechasValidator.ts
  utils/cedula.ts, legacy.ts, fechas.ts
supabase/migrations/
  0001_init.sql
  0002_seed.sql
  0003_booking_constraints.sql
  0004_seed_feriados.sql
3. RUTAS API NECESARIAS
Especifica:

Método HTTP

Path

Body/Query params

Response shape

Validaciones en request

Lógica de negocio crítica

Ejemplo formato esperado:
POST /api/reservas/create Body: { nombre, cedula, habitacionId, checkIn, checkOut, personas } Response: { success, reservaId, costo, error? } Validaciones: cédula válida, habitación existe, fechas no solapan

4. CONFIGURACIÓN SUPABASE
Mi Project URL en Supabase es el siguiente:
https://cgzohxiiopsopubcbgcs.supabase.co

Pasos de setup (CLI, credenciales, conexión)

Orden de aplicación de migraciones

Seeding de datos (habitaciones, feriados)

RLS (Row Level Security) si aplica

5. LÓGICA DE TARIFAS
Las tarifas son las siguientes:

Temporada baja: $25 por noche.

Temporada alta: $40 por noche. Temporada alta = feriados nacionales y del 15 de diciembre al 15 de enero.

Estadías de 7 noches o más: 10% de descuento.

Las reservas de fin de semana tienen tarifa alta siempre y no aplican descuento.

Pseudocódigo o decisión de dónde vive (BD, backend, servicio):

Calcular en JavaScript

Tabla de feriados en BD

Validar los feriados en JavaScript según la tabla y feriados que existan en Ecuador

6. GARANTÍA DE NO SOLAPAMIENTO
ARQUITECTURA DE BD Y NO SOLAPAMIENTO (Riesgo Crítico): Basado en la estructura base, diseña e implementa la solución robusta para garantizar CERO solapamientos bajo concurrencia extrema (DRY, Clean Code):

Constraint de BD: Diseña la migración 0003_booking_constraints.sql usando btree_gist para bloquear cruces. Asegura el edge case donde check-out y check-in son el mismo día (rango semiabierto).

Transaction aislamiento (SERIALIZABLE): Muestra cómo configurar la transacción en backend para aislar concurrencia.

Validación en TS: Implementa rangoFechasValidator.ts para fallar rápido antes de impactar la BD.

Diseña e implementa la solución robusta para el requisito: "6. GARANTÍA DE NO SOLAPAMIENTO" en un sistema de reservas hoteleras. La solución debe garantizar CERO solapamientos de fechas para una misma habitación bajo concurrencia extrema, aplicando principios SOLID, Clean Code y evitando la duplicación de código (DRY).

Escribe el código y la explicación técnica abordando estrictamente los siguientes 4 puntos:

Constraint de BD: Diseña e incluye la migración SQL (ej. usando extensiones como btree_gist o exclusiones a nivel de PostgreSQL) para bloquear físicamente cruces de fechas de reserva en la base de datos.

Transaction aislamiento (SERIALIZABLE): Muestra cómo configurar y ejecutar la transacción en el backend asegurando el nivel de aislamiento SERIALIZABLE o manejo optimista/pesimista para prevenir condiciones de carrera (race conditions).

Validación en app previo: Implementa una función reutilizable y limpia en TypeScript antes de impactar la base de datos para validar los rangos de fechas y fallar rápido.

Edge cases (mismo día check-out/check-in): Asegura que el algoritmo de validación permita correctamente que un huésped haga check-in el mismo día exacto en que otro hace check-out (ej. salida a las 11:00, entrada a las 15:00) sin disparar falsos positivos de solapamiento.

7. INTERFAZ MÍNIMA
HTML plano (sin Tailwind, Bootstrap, estilos CSS)

Solo estructura semántica

Debe mostrar:

Estado de 6 habitaciones

Formulario crear reserva

Listado reservas actuales

Botón cancelar

8. INTEGRACIÓN
Para la comunicación entre canales es necesario que uses @indicium/channel-sync para poder reflejar la disponibilidad hacia los agregadores externos, es decir, cuando ocurra el evento, es necesario que sea disparable después que la base de datos confirme una reserva la cual sea guardada con éxito o sea cancelada oficialmente por medio de los métodos establecidos, enviando al destinatario la información clave: ID de la reserva, #de habitaciones, fechas check-in y check-out y el estado de Confirmed o Canceled en base a la base de datos.

DELIVERABLE ESPERADO:
Diagrama de BD (según los scripts en supabase\migrations\0001_init.sql y supabase\migrations\0002_seed.sql).

Árbol de carpetas con descripción de cada archivo.

Listado de rutas API con especificación completa.

Orden de configuración Supabase (paso a paso).

Pseudocódigo crítico:

Validación cédula ecuatoriana (usando validarCedulaEcuatoriana).

Cálculo de tarifa.

Verificación solapamiento.

Algoritmo de disponibilidad.

Cálculos realizados de forma híbrida para optimización.

HTML mínimo de interfaz (estructura sin estilos).

Decisiones arquitectónicas y trade-offs.

Riesgos identificados y mitigación.

Migraciones propuestas (0003_booking_constraints.sql y 0004_seed_feriados.sql).

Resumen Ejecutivo (utiliza MCP NotebookLM para compilarlo).

RESTRICCIONES:
No generes código completo aún, solo plan/pseudocódigo.

Sé específico en nombres de tablas, columnas, rutas.

Aclara todo lo ambiguo (feriados, capacidad habitaciones, etc.).

Asume: Supabase + Vercel + Node runtime.